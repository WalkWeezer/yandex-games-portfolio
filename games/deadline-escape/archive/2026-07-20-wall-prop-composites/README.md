# Archived: wall×prop composite tiles

Date: 2026-07-20

Pre-baked `tile_wall_{n,s,e,w}_{plant|cooler|cabinet|printer|trash}` and
matching `tile_window_*_*` composites, plus preview sheets.

Feel demo now uses seamless wall strips + separate play props.
Not SoT — do not reload into `refs/sprites/frames/` for the live demo.

Rebuild (if needed): `management/tools/build_wall_prop_tiles.py`
